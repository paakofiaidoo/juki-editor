import React from 'react';

export const PlaceholderPanel = ({ title }: { title: string }) => (
    <div className="p-4 text-gray-500 text-center">
        {title} functionality is not yet implemented.
    </div>
);